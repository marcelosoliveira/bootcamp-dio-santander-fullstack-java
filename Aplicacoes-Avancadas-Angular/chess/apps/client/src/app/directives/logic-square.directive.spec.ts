import { LogicSquareDirective } from './logic-square.directive';

describe('LogicSquareDirective', () => {
  it('should create an instance', () => {
    const directive = new LogicSquareDirective();
    expect(directive).toBeTruthy();
  });
});
